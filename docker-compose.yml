# 已废弃：信令与 TURN 请在独立项目中启动
#
#   cd ../signaling-server
#   docker compose up -d --build
#
#   cd ../turnserver
#   docker compose up -d
#
# 历史一体编排见 docker-compose.legacy.yml

name: remote-control-server-deprecated

services: {}
